# API Contract

All non-health endpoints require `X-Tenant-Id` and `X-User-Id`. `X-Correlation-Id` is accepted from the caller or generated by middleware.

- `POST /api/v1/ai/runs`
- `GET /api/v1/ai/runs/{runId}`
- `GET /api/v1/ai/artifacts/{artifactId}`
- `GET /health/live`
- `GET /health/ready`

