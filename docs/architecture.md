# Architecture

TILSOFTAI uses a controlled pipeline: request context, capability retrieval, agent planning, tool runtime, artifact persistence, sanitization, context packaging, and final answer generation.

The MVP is intentionally limited to the Model domain.

